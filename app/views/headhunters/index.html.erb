<h3><%= t('.hunter_area') %></h3>

<%= link_to  t('.register_new_job'), new_job_path %>
<%= link_to t('.back'), root_path %>
<br />

<% @jobs.each do |job| %>
  <%= render 'jobs/job_card', job: job %>
<% end %>